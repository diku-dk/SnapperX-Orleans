<?xml version="1.0" encoding="utf-8" ?>
<root>
    <!-- ======================================================= -->
    <numGrainPerTxn>            4   </numGrainPerTxn>

    <!-- ======================================================= -->
    <CollectBalance>            100 </CollectBalance>

    <!-- ======================================================= -->
    <numPartitionPerSiloPerTxn> 1   </numPartitionPerSiloPerTxn>
    <numSiloPerTxn>             2   </numSiloPerTxn>

    <!-- ======================================================= -->
    <pactPercent>               50  </pactPercent>
    <multiSiloPercent>          0   </multiSiloPercent>        <!-- cannot do multi-silo PACT on replica -->

    <!-- ======================================================= -->
    <hotGrainPercent>           100 </hotGrainPercent>
    <hotPercentPerPartition>    100 </hotPercentPerPartition>

    <!-- ======================================================= -->
    <actPipeSize>               128 </actPipeSize>
    <pactPipeSize>              128 </pactPipeSize>
    <numActConsumer>            2   </numActConsumer>
    <numPactConsumer>           2   </numPactConsumer>
</root>